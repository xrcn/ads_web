# GFast 3.3.12 前端选择性升级实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan batch-by-batch. Steps use checkbox syntax for tracking.

**Goal:** 将 GFast 3.3.12 的上传、401 和暗色主题修复合入 ADS 前端，保留现有业务与环境配置，并达到 LOCAL_VERIFIED。

**Architecture:** 使用一个无新增依赖的 Node verifier 固定关键源码契约，再按上游 9847c88 做最小 hunk 合并。公共上传组件不整文件覆盖，保留 ADS 的服务端上传配置读取和现有 props 契约。

**Tech Stack:** Vue 3、TypeScript、Vite 7.1.11、Element Plus 2.11.1、Node 20.19+。

## Global Constraints

- 仓库固定为 E:\ads\ads_web；实施前重新确认 develop、HEAD 8558a6854ef71a7970854c0c4d54562b8858fd55 和 dirty 边界。
- 不修改依赖版本、.env、API/WebSocket 地址、微信业务页面或可视化控制台。
- 不引入 test runner、browser automation 或其他新依赖。
- 不执行 npm run lint-fix，避免格式化无关文件。
- 未获授权不执行 commit、push、merge、rebase 或发布。
- 完整输出保存到 E:\ads\.artifacts\gfast-3.3.12-upgrade\frontend\。

---

### Task 1: 建立前端升级 RED verifier

**Files:**
- Create: scripts/verify-gfast-3312-upgrade.mjs

**Interfaces:**
- Consumes: 四个目标源码文件。
- Produces: 失败时非零退出码，成功时输出 assertion 数量。

- [ ] **Step 1: 编写无依赖 verifier**

    import assert from 'node:assert/strict';
    import { readFileSync } from 'node:fs';

    const files = {
      request: readFileSync('src/utils/request.ts', 'utf8'),
      uploadFile: readFileSync('src/components/uploadFile/index.vue', 'utf8'),
      uploadImg: readFileSync('src/components/uploadImg/index.vue', 'utf8'),
      theme: readFileSync('src/theme/el-ui.scss', 'utf8'),
    };

    const checks = [
      [files.request.includes('let isRelogin = false'), '401 single-dialog guard'],
      [files.request.includes('__requestToken'), 'request token snapshot'],
      [files.request.includes('requestToken !== currentToken'), 'refreshed-token retry'],
      [files.uploadFile.includes('watch(() => props.modelValue'), 'file controlled state'],
      [files.uploadFile.includes('_.isEqual(dataFileList.value, transformed)'), 'file deep equality'],
      [files.uploadImg.includes('watch(() => props.modelValue'), 'image controlled state'],
      [files.uploadImg.includes("rawFile.type.substring(0, 5) !== 'image'"), 'image type guard'],
      [files.uploadImg.includes('rawFile.size / 1024 / 1024 > props.uploadSize'), 'image size guard'],
      [files.theme.includes('--el-card-bg-color: var(--el-bg-color-overlay)'), 'dark card background'],
      [files.theme.includes('background-color: var(--el-bg-color-overlay, #ffffff)'), 'message background'],
    ];

    for (const [passed, name] of checks) assert.ok(passed, name);
    console.log('gfast-3.3.12 verifier: ' + checks.length + ' assertions passed');

- [ ] **Step 2: 运行 RED verifier**

Run:

    node scripts/verify-gfast-3312-upgrade.mjs

Expected: FAIL，第一处缺失为 401 single-dialog guard。输出写入 frontend/01-verifier-red.log。

---

### Task 2: 401 单弹窗与刷新 Token 重试

**Files:**
- Modify: src/utils/request.ts
- Test: scripts/verify-gfast-3312-upgrade.mjs

**Interfaces:**
- Consumes: 请求发出时和响应时的 Session token。
- Produces: 新 Token 出现时重试原请求；同一轮失效只弹一个重新登录窗口。

- [ ] **Step 1: 合入请求 Token 快照**

在 request interceptor 将当前 Token 保存到 request config 私有字段，并继续设置 Authorization header。不得改变 base URL、timeout、序列化规则和正常响应结构。

- [ ] **Step 2: 合入 401 处理**

增加模块级 isRelogin。401 时先比较 __requestToken 与当前 Token；不同且两者非空时调用 service(response.config) 重试。否则首次弹窗、后续 401 直接 reject，避免重复弹窗。不得为同一旧 Token 自动循环重试。

- [ ] **Step 3: 运行局部 verifier**

Run:

    node scripts/verify-gfast-3312-upgrade.mjs

Expected: 401 三项通过，随后在上传断言处失败。输出写入 frontend/02-request-green-partial.log。

---

### Task 3: 文件与图片上传受控状态修复

**Files:**
- Modify: src/components/uploadFile/index.vue
- Modify: src/components/uploadImg/index.vue
- Test: scripts/verify-gfast-3312-upgrade.mjs

**Interfaces:**
- Consumes: 现有 modelValue、upload props、服务端上传配置和返回结构。
- Produces: 稳定的 file list 回显、删除、预览、类型/大小/数量校验。

- [ ] **Step 1: 将 computed 副作用改为 watch**

两个组件使用 ref 保存 dataFileList，watch modelValue 并 immediate 执行，深拷贝并补齐 URL，仅在 lodash isEqual 判定内容变化时更新。保留现有 getBatchByKey 配置加载及 fallback，不照抄厂商删除这些行为的整文件版本。

- [ ] **Step 2: 合入上传结果同步**

成功回调移除当前临时 uid，追加服务端返回的 name/path/type/size，再 emit update:modelValue。删除基于当前展示列表生成新数组并 emit；预览继续使用完整 URL。

- [ ] **Step 3: 合入图片边界校验**

图片上传前验证 MIME 前缀为 image，并校验 rawFile.size / 1024 / 1024 不超过 props.uploadSize；数量超限使用现有消息风格提示。单图同步 imageUrl，多图保持 picture-card。

- [ ] **Step 4: 运行 verifier 和构建**

Run:

    node scripts/verify-gfast-3312-upgrade.mjs
    npm run build

Expected: 上传断言通过；主题尚未合入时 verifier 在主题断言失败；build exit code 0。输出写入 frontend/03-upload-check.log 和 frontend/03-build.log。

---

### Task 4: 暗色主题背景修复

**Files:**
- Modify: src/theme/el-ui.scss
- Test: scripts/verify-gfast-3312-upgrade.mjs

- [ ] **Step 1: 合入两个变量替换**

暗色卡片背景改为：

    --el-card-bg-color: var(--el-bg-color-overlay) !important;

消息背景改为：

    background-color: var(--el-bg-color-overlay, #ffffff) !important;

不改其他全局主题规则。

- [ ] **Step 2: 运行完整 verifier**

Run:

    node scripts/verify-gfast-3312-upgrade.mjs

Expected: gfast-3.3.12 verifier: 10 assertions passed。输出写入 frontend/04-verifier-green.log。

---

### Task 5: 前端构建、交互与边界审计

**Files:**
- Verify: 全仓
- Update only if new verified knowledge exists: memory.md

- [ ] **Step 1: fresh 构建**

Run:

    npm run build

Expected: exit code 0。完整输出写入 frontend/05-build.log；对话只报告 exit code 和首个关键错误。

- [ ] **Step 2: 本地页面 smoke**

启动现有本地前端并使用已登录测试环境检查：登录页、用户列表、角色列表、单图上传、多图上传、附件上传、初始化回显、删除确认、预览、暗色消息提示。记录 DOM、Network 和控制台结果；不对生产页面做写操作。

- [ ] **Step 3: 检查 diff 与 Git 边界**

Run:

    git diff --check
    git status --short --branch --untracked-files=all
    git diff --stat
    git diff -- .env .env.development .env.production public/wechat-visual-console

Expected: 只包含设计、计划、verifier 与四个目标源码文件；环境和可视化控制台无变化。

- [ ] **Step 4: 更新验证事实并报告**

只有出现重新验证的新经验或坑时才最小更新 memory.md。最终报告状态、改动、验证、证据、未验证/风险、Git/发布；不执行 commit 或发布。
